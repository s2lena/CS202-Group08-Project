working copy ---changing state----> staging ------commit---> local repo


(git init, git remote add origin {url})
git clone 
git pull 

git add .
git commit -m 
git push (-set--upstream) orgin branch

git checkout -b branch (tạo mới và nhảy vào branch)
git branch ... (tạo branch mới)
git checkout ... (nhảy vào branch)



git log (check commit in local)
git status